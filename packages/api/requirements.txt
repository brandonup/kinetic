# Kinetic API — Python dependencies
# KIN-252: auth service port

# Web framework
fastapi>=0.109
uvicorn[standard]>=0.27

# Validation / settings
pydantic>=2.5
pydantic-settings>=2.1

# JWT verification
PyJWT>=2.8
cryptography>=42.0        # required by PyJWT for RS256/ES256 algs

# HTTP client (JWKS fetch in supabase_jwt.py)
httpx>=0.26

# Supabase Python client (used in deps.py for admin role check)
supabase>=2.3

# Testing
pytest>=7.4
pytest-asyncio>=0.23

# LLM abstraction
litellm>=1.30

# Ingestion pipeline
unstructured[all-docs]>=0.12
google-genai>=1.0
openai>=1.14
tenacity>=8.2
python-multipart>=0.0.9

# Scheduled KB scraping (KIN-459)
apscheduler>=3.10
feedparser>=6.0
html2text>=2024.2        # KIN-480: substack scraper HTML→text conversion
beautifulsoup4>=4.12     # KIN-480: substack scraper body extraction (also transitive via unstructured, pinning explicitly)

# FastAPI TestClient transport
# httpx already listed above — starlette TestClient uses it automatically
# cache-bust: 2026-05-23T17:30:00Z (KIN-480 fresh build with scraper deps)
