---
Status: Complete
Ticket: KIN-329
Date: 2026-03-24
---

# KIN-329 — MCP Server Tests: Results

## Summary

All acceptance criteria met. 78 MCP tests pass (58 in `test_mcp.py`, 20 in `test_mcp_tokens.py`). Full suite: 325 passed, 6 skipped.

## Acceptance Criteria Checklist

| Criterion | Status | Notes |
|---|---|---|
| Auth: valid token → 200 | ✅ | `test_valid_token_allows_request` |
| Auth: invalid token → 401 | ✅ | `test_token_not_in_db_returns_401` |
| Auth: revoked token → 401 | ✅ | `test_revoked_token_returns_401` |
| Auth: `last_used_at` updated on success | ✅ | `TestMCPLastUsedAt::test_last_used_at_update_is_scheduled_on_successful_auth` (added this session) |
| Auth: missing header → 401 | ✅ | `test_missing_authorization_header_returns_401` |
| Scope: no scope params → 400 missing_scope | ✅ | `test_no_scope_params_returns_400` |
| Scope: malformed UUID → 400 | ✅ | `test_invalid_uuid_*` (3 tests) |
| Scope: non-existent entity → 404 | ✅ | `test_entity_not_found_*` (3 tests) |
| Scope: all 7 valid combinations correct | ✅ | `TestMCPScopeRouting` (7 tests) |
| ACL: private project non-owner → 404 | ✅ | `test_project_not_owned_returns_404` |
| ACL: public agent any user → 200 | ✅ | `test_public_agent_not_owned_is_accessible` |
| ACL: private agent non-owner → 404 | ✅ | `test_private_agent_not_owned_returns_404` |
| ACL: company non-owner → 404 | ✅ | `test_company_not_owned_returns_404` |
| ACL: owner private agent → 200 | ✅ | `test_owner_accessing_own_private_agent_returns_200` |
| Rate limit: 1001st request → 429 + headers | ✅ | `TestMCPRateLimitHeaders` (4 tests) |
| Rate limit: per-user cap override | ✅ | `test_per_user_cap_override_respected` |
| Rate limit: UTC midnight reset | ✅ | `test_counter_resets_at_utc_midnight` |
| Context assembly: correct layers per scope | ✅ | All 7 scope combinations verified with layer assertions |
| Context assembly: framework match → L7 + ID | ✅ | `test_agent_scope_assembles_l7_on_framework_match` |
| Context assembly: no match → L7 omitted, null | ✅ | `test_agent_scope_omits_l7_on_no_framework_match` |
| Context assembly: RAG miss → sources [] | ✅ | `test_rag_miss_returns_empty_sources` |
| 80%+ line coverage on MCP module | ✅ | All branches covered including fail-open RPC error path |
| Rate limit reset tested (mock UTC midnight) | ✅ | RPC mock returns `allowed=True, request_count=1` for new day |

## Additional Test Added (Bonus)

`TestMCPRateLimitFailOpen::test_rate_limit_rpc_error_fails_open` — verifies that a RuntimeError from the rate limit RPC causes fail-open behavior (request proceeds). Covers the `except Exception` branch in `_check_rate_limit`.

## Files Modified

- `packages/api/tests/test_mcp.py` — added `TestMCPLastUsedAt` (2 tests) and `TestMCPRateLimitFailOpen` (1 test)

## Recommendation

**Ship.** All acceptance criteria met. No bugs found in the MCP implementation during test coverage verification.
